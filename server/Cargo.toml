[package]
name = "symbols"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Self-hosted debug-symbol server speaking the debuginfod protocol, with OIDC-authenticated publishing from CI."

[dependencies]
actix-web = "4.13"
async-compression = { version = "0.4", features = ["tokio", "gzip"] }
base64 = "0.23"
bytes = "1"
chrono = { version = "0.4", features = ["serde"] }
clap = { version = "4.6", features = ["derive"] }
# The management ACL is a filt-rs expression, parsed straight out of the
# config file (serde) and able to match claims with regexes.
filt-rs = { version = "1.1", features = ["serde", "regex"] }
flate2 = "1"
futures = "0.3"
hex = "0.4"
humantime-serde = "1.1"
# rust_crypto selects the process-level crypto provider; without exactly one
# provider feature, signing/validation panics at runtime.
jsonwebtoken = { version = "11", features = ["rust_crypto"] }
memmap2 = "0.9"
object = { version = "0.40", default-features = false, features = ["read"] }
object_store = { version = "0.14", features = ["aws"] }
pdb = "0.8"
rand = "0.10"
reqwest = { version = "0.12", default-features = false, features = ["rustls-tls", "stream", "json"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
serde_yaml = "0.9"
sha2 = "0.11"
symbols-ui = { path = "../ui" }
tempfile = "3"
thiserror = "2"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "signal", "time"] }
tokio-util = { version = "0.7", features = ["io"] }
tracing = "0.1"
tracing-batteries = { git = "https://github.com/sierrasoftworks/tracing-batteries-rs.git", features = [
  "opentelemetry",
] }
url = "2"
